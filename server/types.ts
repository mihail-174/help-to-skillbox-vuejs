export type Course = {
    id: number,
    name: string,
    categoryId: number
}
export type Category = {
    id: number,
    name: string,
    colorId: number
}
export type Color = {
    id: number,
    code: string
}